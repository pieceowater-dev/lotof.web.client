query AdminDeepLinks($categoryId: ID) {
  adminDeepLinks(categoryId: $categoryId) {
    id
    code
    categoryId
    target
    label
    clickCount
    registrationCount
    appInstallCount
    createdAt
  }
}
