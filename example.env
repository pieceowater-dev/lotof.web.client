## Vite-exposed env vars used by the app (loaded at build/dev startup)
# Hub Gateway base URL (used for GraphQL /query and OAuth redirect)
VITE_API_HUB=http://localhost:8080

# Atrace Gateway base URL (used for GraphQL /query)
VITE_API_ATRACE=http://localhost:8081

# Note: after changing these, restart the dev server (they are read at startup).
